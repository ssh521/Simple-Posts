<?php

return [
    /*
    |--------------------------------------------------------------------------
    | 테이블명
    |--------------------------------------------------------------------------
    |
    | 게시글을 저장할 데이터베이스 테이블명을 설정합니다.
    | 기본값: 'simple_posts'
    |
    */
    'table_name' => env('SIMPLE_POSTS_TABLE_NAME', 'simple_posts'),

    /*
    |--------------------------------------------------------------------------
    | 라우트 설정
    |--------------------------------------------------------------------------
    |
    | 패키지의 라우트 관련 설정입니다.
    |
    */
    'route' => [
        'prefix' => env('SIMPLE_POSTS_ROUTE_PREFIX', 'posts'),
        'name' => env('SIMPLE_POSTS_ROUTE_NAME', 'posts.'),
        'middleware' => env('SIMPLE_POSTS_MIDDLEWARE', []),
    ],

    /*
    |--------------------------------------------------------------------------
    | 페이지네이션
    |--------------------------------------------------------------------------
    |
    | 게시글 목록에서 한 페이지에 표시할 게시글 수를 설정합니다.
    | 기본값: 10
    |
    */
    'pagination' => [
        'per_page' => env('SIMPLE_POSTS_PER_PAGE', 10),
    ],

    /*
    |--------------------------------------------------------------------------
    | 날짜 형식
    |--------------------------------------------------------------------------
    |
    | 게시글 날짜 표시 형식을 설정합니다.
    | 기본값: 'Y-m-d'
    |
    */
    'date_format' => env('SIMPLE_POSTS_DATE_FORMAT', 'Y-m-d'),

    /*
    |--------------------------------------------------------------------------
    | 유효성 검사 규칙
    |--------------------------------------------------------------------------
    |
    | 게시글 생성/수정 시 적용할 유효성 검사 규칙을 설정합니다.
    |
    */
    'validation' => [
        'title' => [
            'required',
            'string',
            'max:255',
        ],
        'content' => [
            'required',
            'string',
        ],
        'date' => [
            'required',
            'date',
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | 뷰 설정
    |--------------------------------------------------------------------------
    |
    | 뷰 관련 설정입니다.
    |
    */
    /*
    |--------------------------------------------------------------------------
    | 마이그레이션 자동 로드
    |--------------------------------------------------------------------------
    |
    | 패키지 로드 시 마이그레이션을 자동으로 로드할지 설정합니다.
    | false로 설정하면 수동으로 마이그레이션을 publish해야 합니다.
    |
    */
    'auto_load_migrations' => env('SIMPLE_POSTS_AUTO_LOAD_MIGRATIONS', true),

    /*
    |--------------------------------------------------------------------------
    | 뷰 설정
    |--------------------------------------------------------------------------
    |
    | 뷰 관련 설정입니다.
    |
    */
    'views' => [
        'layout' => env('SIMPLE_POSTS_LAYOUT', 'simple-posts::posts.layout'),
        'theme' => env('SIMPLE_POSTS_THEME', 'bootstrap'), // bootstrap, tailwind 등
    ],
];
